<!--index.wxml-->
<scroll-view enable-back-to-top="true">
	<view wx:for="{{themeList}}" wx:key="index" class="item">
  <image  src="{{item.member.avatar_normal}}" lazy-load="true" class="avatar"></image>
	   <text>{{ item.title}}</text>
	</view>
</scroll-view>
